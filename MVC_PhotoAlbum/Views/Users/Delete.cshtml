@model MVC_PhotoAlbum.Models.User

@{
    ViewBag.Title = "Delete";
}

<h2>刪除</h2>

<h3>你確定要刪除嗎?</h3>
<div>
    <hr />
    <h4>帳號</h4>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Account)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Account)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.role)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.role)
        </dd>
        <span class="field-validation-error text-danger">@ViewBag.BossCanNotDelete</span>
    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="確認刪除" class="btn app-btn-primary" />
            <a href="@Url.Action("Index")" class="btn app-btn-primary">返回</a>
        </div>
    }
</div>
